---
interface Props {
  label: string;
  counter?: string;
  id?: string;
}

const { label, counter, id } = Astro.props;
---

<div class="section-band" id={id}>
  <span class="section-label">{label}</span>
  <span class="section-counter">{counter}</span>
</div>
